#include <bits/stdc++.h>
using namespace std;

class Node{
public:
    int data;
    Node* next;

    Node(int data){
       this-> data= data;
       this -> next = NULL;
   }


};

Node* insertAttail(Node* &tail, int d){
    Node* temp = new Node(d);
       Tail -> next = temp;
       tail = temp;
    }
}


int main(){
    Node* temp = new Node(1);
    Node* head = temp;
    Node* tail = temp;



}
